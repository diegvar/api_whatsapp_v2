# Puerto del servidor
PORT=3002

# Token para autenticar las peticiones a la API (cambiar por uno seguro)
TOKEN_VAL=tu_token_secreto_aqui

# Entorno (opcional: development | production)
NODE_ENV=development
